**Meta：前言。** 与先前篇章所提到的调试类似，我认为另一个值得单独拿出来整理的话题是，Coq的项目里面会有哪一些“常见”的套路和写法。对这类写法的归纳不会出现在Coq所使用的目的——发展形式化的数学/PL理论上，但不论是自己写一门东西，亦或查阅他人的代码，这些“基础”的用法都可能可以作为一个人迅速上手的“接入口”：

> **TODO：Coq中“常见”的设计模式** 
> **TODO：调查Program Transformation的种类**
>  - 介绍单纯为了模拟一个Class可能会遇到的坑，介绍Module此时的用法
>  - Typeclass机制
>  - Section机制
>  - Inductive type

# 1. Inductive Type
作为Coq通常教材中的基础概念，本文不会介绍Inductive type。但是，它将被用作一个引子去理解其它概念。

为了理解概念，我们需要知道这些概念是为什么问题服务的。接下来的主要问题将会是：我们如何parse一个东西，parse出来的结果有什么特点。

想象一下你有一条式子
```
(1 + 2 = 0)
```
然后要去parse它，那么你可能会用Coq中的Inductive type这样子去描述：
```
Definition term := nat.

Inductive Expr : Set :=
| Add : Expr -> Expr -> Expr
| Eq : Expr -> Expr -> Expr
| Term : nat -> Expr
.
```

那么在这种定义中，上面的式子会被表达成如下的一个Coq term：
```
Definition example_1 : Expr := Eq (Add (Term 1) (Term 2)).
```

*TODO：填充评论*

接下来我们换一种语言再parse一遍。

*TODO：用java写一遍这些东西*